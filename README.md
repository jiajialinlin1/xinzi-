# Xinzi

macOS menu bar desktop app scaffold, built with Electron.

## Development

```bash
npm install
npm run dev:desktop
```

## Build

```bash
npm run build:mac
```

The macOS app bundle is generated in `dist/mac-arm64`.
